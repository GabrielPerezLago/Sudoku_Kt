<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sudoku"
        android:usesCleartextTraffic="true">
        <!--<activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Sudoku">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />


            </intent-filter>
        </activity>-->

        <activity android:name=".views.InicioActivity" android:exported="true" android:theme="@style/Theme.Sudoku">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".views.ModeActivity" android:theme="@style/Theme.Sudoku"/>
        <activity android:name=".views.NombreActivity" android:theme="@style/Theme.Sudoku"/>
        <activity android:name=".views.SudokuActivity" android:theme="@style/Theme.Sudoku"/>
        <activity android:name=".views.PuntuacionesActivity" android:theme="@style/Theme.Sudoku"/>
    </application>

</manifest>